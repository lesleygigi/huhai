# 020 国内账号密码云存档实施拆分

## 背景

[task/019.md](/data/2t/projects_moved/travel_project/jll/huhai/task/019.md#L1) 已经完成“国内静态托管 + 国内认证 / 数据库”的方案设计。

为了避免后续实施时范围过大、控制台配置与代码改动交叉、联调顺序混乱，需要把 `019` 拆成明确的实施步骤。

本任务文档即为 `019` 的 step by step 落地版。

---

## 总目标

围绕当前项目，完成以下目标：

1. 前端部署切换到 CloudBase 静态网站托管
2. 接入 CloudBase 认证
3. 接入每用户 6 槽位云存档
4. 保留本地存档作为兜底
5. 支持手机浏览器访问
6. 支持中国大陆用户在无 VPN 条件下正常使用

---

## Step 1：锁定实施口径与边界

### 目标

先把这轮实施的边界固定，避免开发到一半又临时切换产品路线。

### 需要完成的事

1. 确认正式技术选型：
   - 前端托管：`CloudBase 静态网站托管`
   - 认证与数据库：`CloudBase`
2. 确认第一版账号形态：
   - 底层登录标识：邮箱
   - UI 文案可写“账号”
3. 确认第一版存档形态：
   - 每用户 6 个云槽位
   - 保留本地存档
   - 同步规则为“最后写入覆盖”
4. 明确第一版不做：
   - 自定义用户名体系
   - 社交登录
   - 成就系统
   - 后台管理页
   - 多端冲突合并

### 重点文件

- [task/019.md](/data/2t/projects_moved/travel_project/jll/huhai/task/019.md#L1)

### 产出

- 一份固定的实施口径

### 验收标准

- 后续所有代码与配置均按本步口径执行
- 不再把“邮箱账号 / 用户名账号 / 游客账号”混着做

### 执行结果（已完成）

本步已完成，现将本轮实施口径正式锁定如下。

#### 1. 技术选型定稿

- 前端托管：`CloudBase 静态网站托管`
- 认证与数据库：`CloudBase`
- 本地存档：继续保留
- 云存档：新增并作为登录用户主存档来源

#### 2. 第一版账号口径定稿

- 底层登录标识：邮箱
- UI 文案：可以写“账号”，但底层按邮箱实现
- 密码：用户自行设置并用于后续登录
- 昵称 / 展示名：不纳入第一版阻塞范围

#### 3. 第一版存档口径定稿

- 每个登录用户拥有 6 个云存档槽位
- 本地存档继续存在，作为断网与未登录兜底
- 云端同步规则采用“最后写入覆盖”
- 第一版不处理复杂冲突合并

#### 4. 第一版明确排除项

以下内容不进入当前实施范围：

- 自定义用户名体系
- 社交登录
- 游客转正式账号
- 成就系统
- 后台管理页
- 多设备同时在线冲突合并
- 云端设置同步

#### 5. 后续执行约束

从 Step 2 开始，后续所有工作都必须遵守以下约束：

1. 不再把“用户名登录”作为第一版必做目标
2. 不再引入海外静态托管或海外认证数据库作为正式版主方案
3. 不再把本地存档改造成“仅云端可用”
4. 不在正式云存档完成前扩张到成就、统计、后台等附加系统

Step 1 结论：实施边界已固定，后续 Step 2 至 Step 16 一律以本节口径为准。

---

## Step 2：准备腾讯云侧资源

### 目标

先把云侧资源创建出来，避免代码已经写完但没有可接入环境。

### 需要完成的事

1. 注册并登录腾讯云控制台
2. 开通或创建 CloudBase 环境
3. 记录环境标识：
   - `envId`
4. 确认是否启用：
   - 身份认证
   - 云数据库
5. 开通 CloudBase 静态网站托管
6. 记录静态托管默认域名

### 推荐记录信息

建议把以下信息单独保存到项目外部的安全记录中：

```text
腾讯云账号主体
CloudBase 环境名
CloudBase envId
CloudBase 静态托管默认域名
是否启用静态网站托管
```

### 产出

- 可用的 CloudBase 环境
- 可用的 CloudBase 静态网站托管实例

### 验收标准

- 已能进入 CloudBase 控制台管理页面
- 已能找到环境 `envId`
- 已能进入 CloudBase 静态网站托管页面

### 执行结果（已完成）

本步已完成。仓库内准备项已补齐，且云侧关键资源信息已经拿到。当前已落实如下：

#### 1. 仓库内准备项已补齐

- 已新增资源记录模板：
  - [task/020_resource_record.md](/data/2t/projects_moved/travel_project/jll/huhai/task/020_resource_record.md#L1)
- 已新增环境变量样板：
  - [.env.example](/data/2t/projects_moved/travel_project/jll/huhai/.env.example#L1)

#### 2. 已确认的项目现状

- 仓库当前没有现成的 `.env` 模板
- `vite.config.ts` 目前没有自定义环境变量逻辑，适合后续直接接入 `VITE_` 前缀配置
- `package.json` 的构建流程已固定为：
  - `npm run story:build`
  - `tsc -b`
  - `vite build`

#### 3. 已记录的云侧关键资源

- CloudBase `envId`：`test-d7gmoq0dn303446ba`
- CloudBase 静态托管默认域名：`test-d7gmoq0dn303446ba-1422160391.tcloudbaseapp.com`
- 历史联调用 EdgeOne 预览域名：`huhai-zc6bffk7.edgeone.cool`（不再作为主发布入口）

对应记录已写入：
- [task/020_resource_record.md](/data/2t/projects_moved/travel_project/jll/huhai/task/020_resource_record.md#L1)

#### 4. 仍建议后续补全的资源信息

虽然 Step 2 已可视为完成，但后面正式联调和发布前，仍建议继续把下列字段补完整：

- 腾讯云账号主体
- CloudBase 环境名
- 所在地域
- 身份认证是否已开通
- 云数据库是否已开通
- 静态网站托管是否已开通
- 静态托管默认域名

Step 2 结论：**认证、数据库、静态托管默认域名都已具备。** 后续可以直接推进 CloudBase 静态托管发布链路。

---

## Step 3：配置 CloudBase 认证

### 目标

先让认证单独跑通，再接存档。

### 需要完成的事

1. 在 CloudBase 控制台开启登录方式
2. 确认第一版启用：
   - 邮箱登录
3. 确认是否需要保留：
   - 匿名登录
   - 建议：第一版可不开，避免后续匿名转正式用户逻辑过早膨胀
4. 配置安全来源
   - 本地域名
   - 当前测试来源
   - CloudBase 静态托管默认域名
5. 明确找回密码和邮箱验证所依赖的页面跳转路径

### 实施建议

先只做这三条链路：

1. 获取邮箱验证码
2. 注册并设置密码
3. 邮箱 + 密码登录

暂时不要在这一步处理云存档。

### 产出

- CloudBase 认证配置完成

### 验收标准

- 控制台中登录方式已开启
- 安全来源已配置
- 前端将来可在本地联调时通过来源校验

### 执行结果（已完成）

本步已完成实施层面的定稿与操作清单整理，结果见：

- [task/020_step3_cloudbase_auth_checklist.md](/data/2t/projects_moved/travel_project/jll/huhai/task/020_step3_cloudbase_auth_checklist.md#L1)

本次确认的关键结论如下：

#### 1. 第一版只启用邮箱登录

CloudBase 官方文档明确支持邮箱登录，并说明邮箱登录可用于：

- 邮箱注册
- 注册时设置密码
- 邮箱 + 密码登录
- 更新登录邮箱和密码 citeturn0search1turn1search3

因此本项目第一版认证方式锁定为：

- 启用：邮箱登录
- 不启用 / 不依赖：用户名密码登录、匿名登录、自定义登录

#### 2. SMTP 是硬前提

CloudBase 官方文档明确要求：开启邮箱登录前，需要先配置发件邮箱 SMTP。 citeturn0search1

这意味着 Step 3 不能只靠“打开邮箱登录开关”完成，必须先完成：

1. 发件邮箱准备
2. SMTP 参数填写
3. 发件人配置保存

#### 3. 安全来源已明确

当前应加入的安全来源口径已定为：

- `localhost:5173`
- CloudBase 静态托管默认域名（待开通后补入）

EdgeOne 预览域名不再作为正式口径保留。

#### 4. 当前仍需你在控制台执行的动作

由于 CloudBase 控制台配置必须使用你的腾讯云账号完成，我无法直接代你执行。你现在需要按清单文件中的顺序，在控制台里完成：

1. 配置 SMTP
2. 开启邮箱登录
3. 配置安全来源
4. 保持用户名密码登录与匿名登录不启用

#### 5. 已确认完成的控制台配置

你已完成以下实际控制台操作：

1. 已配置 SMTP 发件邮箱
2. 已开启邮箱登录
3. 已在 `环境管理 -> HTTP 访问服务 -> 跨域设置` 中加入：
   - `localhost:5173`
   - `huhai-zc6bffk7.edgeone.cool`
4. 已保持：
   - 用户名密码登录未启用
   - 匿名登录未启用

需要注意的是，在你当前这套 CloudBase 控制台中，Web 侧允许来源的相关配置实际落在：

- `环境管理 -> HTTP 访问服务 -> 跨域设置`

而不是此前误判的“环境管理 -> 安全管控”。

补充说明：

- 由于当前方案已切换为 CloudBase 静态托管，等默认域名开通后，还需要把该默认域名继续加入同一处跨域设置。

Step 3 结论：**认证配置已具备第一版接入条件。** 切换静态托管后只需补加默认域名即可。

---

## Step 4：设计云存档集合结构

### 目标

在写前端代码前，先把云端数据结构定死。

### 需要完成的事

1. 在 CloudBase 数据库中创建 `save_slots` 集合
2. 明确每条记录字段：

```ts
type CloudSaveSlot = {
  _id?: string;
  userId: string;
  slotId: number;
  version: number;
  chapterName: string;
  sceneName: string;
  inkStateJson: string;
  history: unknown[];
  dialogueHistory: unknown[];
  savedAt: string;
  createdAt: string;
  updatedAt: string;
};
```

3. 明确业务约束：
   - 每个用户最多 6 条当前槽位记录
   - 每个 `slotId` 只保留 1 条有效记录
4. 定义保存策略：
   - 保存即覆盖同一用户同一槽位旧记录

### 注意点

CloudBase 的数据库底层更接近文档集合，不是传统 SQL 表。  
因此这一步要先把“文档结构”和“唯一性约束的业务实现”想清楚，避免后面前端逻辑和数据库结构打架。

### 产出

- 一个固定的云存档文档结构

### 验收标准

- 前端和云端对字段命名一致
- 不再出现“本地 `chapterName`，云端叫 `chapter_name`”之类的双命名

### 执行结果（已完成实施定稿）

本步已完成，云存档集合结构已经固定，结果见：

- [task/020_step4_save_schema.md](/data/2t/projects_moved/travel_project/jll/huhai/task/020_step4_save_schema.md#L1)

本次定稿的核心内容如下：

#### 1. 集合名定稿

第一版云存档集合统一命名为：

- `save_slots`

后续不再使用其他变体名称。

#### 2. 文档结构定稿

云端文档结构已固定为：

```ts
type CloudSaveSlot = {
  _id?: string;
  userId: string;
  slotId: number;
  version: number;
  chapterName: string;
  sceneName: string;
  inkStateJson: string;
  history: unknown[];
  dialogueHistory: unknown[];
  savedAt: string;
  createdAt: string;
  updatedAt: string;
};
```

#### 3. 命名口径定稿

字段一律使用 camelCase，与当前本地 `SaveData` 对齐：

- `chapterName`
- `sceneName`
- `inkStateJson`
- `dialogueHistory`

不再改写成下划线命名。

#### 4. 业务约束定稿

已固定以下约束：

1. 每个用户最多 6 个槽位
2. 每个 `userId + slotId` 只保留 1 条当前有效记录
3. 保存策略为“同槽位覆盖”，不是每次新增一条历史文档

#### 5. 时间字段定稿

时间统一使用 ISO 字符串：

- `savedAt`
- `createdAt`
- `updatedAt`

#### 6. 控制台侧后续动作

本步已经完成结构设计，但 CloudBase 集合的实际创建仍需在控制台中完成。控制台创建时应严格按本步 schema 文档命名：

- 集合名：`save_slots`

Step 4 结论：**云存档结构已锁定。** 后续 Step 5、Step 9、Step 10 一律以该 schema 文档为准。

---

## Step 5：配置 CloudBase 安全规则

### 目标

在写读写代码前，先把“用户只能访问自己的存档”锁好。

### 需要完成的事

1. 给 `save_slots` 集合配置安全规则
2. 规则目标：
   - 未登录用户不能读写 `save_slots`
   - 已登录用户只能访问 `userId == 当前用户 ID` 的记录
3. 单独考虑匿名登录是否允许访问
   - 如果第一版不开匿名登录，则直接不处理匿名权限

### 规则思路

本步不要求先写极复杂表达式，但必须满足：

1. “不能越权看别人”
2. “不能伪造写入别人的 `userId`”

### 产出

- 云存档集合的安全规则

### 验收标准

- 登录用户 A 看不到用户 B 数据
- 未登录用户无法直接访问云存档集合

### 执行结果（已完成实施定稿）

本步已完成，云存档安全规则口径已经固定，结果见：

- [task/020_step5_security_rules.md](/data/2t/projects_moved/travel_project/jll/huhai/task/020_step5_security_rules.md#L1)

本次定稿的核心内容如下：

#### 1. 规则作用集合定稿

第一版只处理：

- `save_slots`

#### 2. 第一版权限目标定稿

已固定以下权限目标：

1. 未登录用户对 `save_slots` 完全无权
2. 已登录用户只能访问 `userId == 当前用户 ID` 的记录
3. 写入、更新、删除都必须围绕 `userId` 做归属校验

#### 3. 核心安全边界定稿

本步明确锁定的边界是：

- 不能只靠前端过滤来隔离用户数据
- CloudBase 规则层必须实际拦住越权访问
- 必须拦住伪造 `userId` 的写入和更新

#### 4. 联调用例已预先固定

本步已把后续必须验证的安全场景固定下来，包括：

- 未登录读写失败
- 用户 A 不能看用户 B
- 用户 A 不能伪造 `userId`
- 用户 A 不能删除或修改用户 B 的记录

#### 5. 控制台侧后续动作

本步完成的是“规则目标定稿”，不是前端代码实现。  
后续在 CloudBase 控制台真正配置 `save_slots` 规则时，必须以本步文档为准。

Step 5 结论：**云存档权限边界已锁定。** 后续 Step 6、Step 9、Step 15 一律以本步规则文档为准。

---

## Step 6：前端安装 CloudBase SDK 并初始化

### 目标

把云环境正式接入到前端工程。

### 需要完成的事

1. 安装依赖：
   - `@cloudbase/js-sdk`
2. 新增客户端初始化模块：
   - [src/lib/cloudbase.ts](/data/2t/projects_moved/travel_project/jll/huhai/src/lib)
3. 封装以下能力：
   - 初始化 `app`
   - 获取 `auth`
   - 获取数据库实例
4. 明确环境变量或配置来源

### 建议新增文件

- `src/lib/cloudbase.ts`

### 实施原则

- 所有 CloudBase 初始化只走一个模块
- 不要在多个组件里到处 `init()`

### 产出

- CloudBase 前端接入基础层

### 验收标准

- 本地开发环境可成功初始化 SDK
- 不出现重复初始化或环境 ID 到处写死的问题

### 执行结果（已完成）

本步已完成，CloudBase 前端初始化层已经落地。

#### 1. 依赖已安装

已新增依赖：

- `@cloudbase/js-sdk`

对应文件：

- [package.json](/data/2t/projects_moved/travel_project/jll/huhai/package.json#L1)
- [package-lock.json](/data/2t/projects_moved/travel_project/jll/huhai/package-lock.json#L1)

#### 2. 初始化模块已新增

已新增统一初始化模块：

- [src/lib/cloudbase.ts](/data/2t/projects_moved/travel_project/jll/huhai/src/lib/cloudbase.ts#L1)

当前已封装：

1. `getCloudbaseEnvId()`
2. `isCloudbaseConfigured()`
3. `getCloudbaseApp()`
4. `getCloudbaseAuth()`
5. `getCloudbaseDatabase()`

其中约束已固定为：

- 全项目只从这一处初始化 CloudBase
- 未配置 `VITE_CLOUDBASE_ENV_ID` 时，只有在真正调用 CloudBase 能力时才抛出明确错误

#### 3. 环境变量类型声明已补齐

已新增：

- [src/vite-env.d.ts](/data/2t/projects_moved/travel_project/jll/huhai/src/vite-env.d.ts#L1)

当前已明确声明：

- `VITE_CLOUDBASE_ENV_ID`
- `VITE_APP_ENV`

#### 4. 现有环境变量样板可直接承接

现有样板文件：

- [.env.example](/data/2t/projects_moved/travel_project/jll/huhai/.env.example#L1)

其中 `VITE_CLOUDBASE_ENV_ID` 已可直接填入：

- `test-d7gmoq0dn303446ba`

#### 5. 构建验证已通过

已执行：

- `npm run build`

结果：

- 构建通过
- 新增 CloudBase 基础层未破坏现有 Vite/TypeScript 构建

Step 6 结论：**CloudBase SDK 已接入工程，基础初始化层已可供 Step 7 与 Step 9 直接复用。**

---

## Step 7：前端接入认证基础能力

### 目标

先把“能注册、能登录、能退出、能恢复会话”做出来。

### 需要完成的事

1. 新增认证模块：
   - `src/engine/auth.ts`
2. 封装函数：

```ts
requestEmailVerification(email)
signUpByEmail(email, password, verificationCode)
signInWithEmailPassword(email, password)
signOut()
getCurrentUser()
```

3. 增加认证状态管理：
   - `authUser`
   - `authStatus`
4. 页面启动时恢复会话

### 建议新增组件

- `src/components/AuthModal.tsx`
- `src/components/AccountPanel.tsx`

### UI 第一版最少要有

1. 注册弹窗
2. 登录弹窗
3. 当前账号信息
4. 退出按钮

### 产出

- 认证功能最小闭环

### 验收标准

- 新用户可注册
- 已注册用户可登录
- 刷新页面后登录态能恢复
- 用户可退出登录

### 执行结果（已完成）

本步已完成，认证基础能力已经接入前端工程。

#### 1. 认证模块已新增

已新增：

- [src/engine/auth.ts](/data/2t/projects_moved/travel_project/jll/huhai/src/engine/auth.ts#L1)

当前已封装：

1. `getCurrentAuthUser()`
2. `signUpWithEmailAndPassword()`
3. `signInWithEmailAndPassword()`
4. `signOutAuth()`
5. `validateEmail()`
6. `validatePassword()`

说明：

虽然最初实现曾临时按“验证邮件跳转”口径接入，但在真实联调后已修正为：

- 注册：先发送邮箱验证码
- 校验：用户输入邮箱验证码完成注册
- 登录：邮箱 + 密码
- 会话恢复：读取当前登录用户

这与 CloudBase 当前实际邮箱注册行为保持一致。

#### 2. 认证组件已新增

已新增：

- [src/components/AuthModal.tsx](/data/2t/projects_moved/travel_project/jll/huhai/src/components/AuthModal.tsx#L1)
- [src/components/AccountPanel.tsx](/data/2t/projects_moved/travel_project/jll/huhai/src/components/AccountPanel.tsx#L1)

当前已提供：

1. 登录弹层
2. 注册弹层
3. 菜单首页账号状态面板
4. 退出登录入口

#### 3. 游戏主界面已接线

已回修：

- [src/components/GameMenu.tsx](/data/2t/projects_moved/travel_project/jll/huhai/src/components/GameMenu.tsx#L1)
- [src/components/GameView.tsx](/data/2t/projects_moved/travel_project/jll/huhai/src/components/GameView.tsx#L1)
- [src/styles/game.css](/data/2t/projects_moved/travel_project/jll/huhai/src/styles/game.css#L1)

当前已实现：

1. 菜单首页显示账号状态
2. 未登录时可打开“登录 / 注册”弹层
3. 已登录时显示当前邮箱并允许退出
4. 页面启动时自动尝试恢复登录态
5. 未配置 `VITE_CLOUDBASE_ENV_ID` 时，账号区会明确提示暂不可用

#### 4. 当前行为口径

注册流程当前分为两段：

1. 先输入邮箱和密码，发送验证码
2. 再输入邮箱验证码完成注册

登录成功后：

- 会刷新当前账号状态
- 关闭认证弹层

退出登录后：

- 当前账号状态清空

#### 5. 构建验证已通过

已执行：

- `npm run build`

结果：

- 构建通过

说明：

- 新增认证模块、弹层和菜单接线未破坏现有项目构建
- 当前构建会出现包体积告警，但不影响构建成功；这属于后续优化项，不阻塞 Step 7

Step 7 结论：**注册、登录、退出、会话恢复的最小认证闭环已经具备。**

---

## Step 8：前端接入找回密码与邮箱验证链路

### 目标

把“能登录”补成“可长期使用的账号系统”。

### 需要完成的事

1. 新增“忘记密码”入口
2. 封装密码重置流程
3. 配置重置密码后的前端回跳页
4. 处理验证成功 / 失败文案

### 注意点

不要把这一步留到最后。  
因为没有找回密码的账号系统，正式上线后用户会立刻遇到支持问题。

### 产出

- 完整密码找回链路

### 验收标准

- 测试邮箱能收到验证码 / 重置流程邮件
- 用户能完成密码重置

---

## Step 9：封装云存档读写模块

### 目标

让前端能以统一接口访问本地存档与云存档。

### 需要完成的事

1. 新增模块：
   - `src/engine/cloudSave.ts`
2. 封装函数：

```ts
listCloudSlots()
loadCloudSlot(slotId)
saveCloudSlot(slotId, saveData)
deleteCloudSlot(slotId)
```

3. 内部完成：
   - 记录查询
   - 新增 / 覆盖保存
   - 删除槽位
4. 把 CloudBase 文档结构映射到现有 `SaveData`

### 要求

- `cloudSave.ts` 不直接操作 UI
- `cloudSave.ts` 只负责数据层

### 产出

- 云存档数据层

### 验收标准

- 给定一个 `SaveData`，可以成功写入对应云槽位
- 给定 `slotId`，可以读回正确的 `SaveData`

---

## Step 10：改造现有存档面板与游戏流程

### 目标

把当前“纯本地存档”改造成“本地 + 云端”的正式交互。

### 需要完成的事

1. 回修 [src/components/GameView.tsx](/data/2t/projects_moved/travel_project/jll/huhai/src/components/GameView.tsx#L321)
2. 保留现有本地存档逻辑：
   - `createSaveData`
   - `writeSave`
   - `readSave`
3. 改成保存时：
   - 先写本地
   - 若已登录，再写云端
4. 改成读档时：
   - 已登录优先读云端
   - 云端失败回退本地
5. 面板新增状态：
   - 当前账号
   - 云同步状态
   - 最近同步时间

### 文案建议

- 未登录：`当前仅保存到本机`
- 已登录：`已同步到云端`
- 云同步失败：`本机已保存，云端同步失败`

### 产出

- 云存档正式接入游戏菜单

### 验收标准

- 玩家在 UI 层能明确知道自己是“本地档”还是“云档”
- 不会误以为已上云，实际只保存在本机

---

## Step 11：本地存档迁移到云端

### 目标

让老玩家或测试玩家已有的本地档能安全迁移。

### 需要完成的事

1. 登录成功后检查本地 6 槽位
2. 检查当前账号云端 6 槽位
3. 当：
   - 云端为空
   - 本地有档
   时，提示是否上传本地档
4. 定义覆盖策略：
   - 第一版默认“云端优先”
   - 仅在明确确认时才用本地覆盖云端

### 产出

- 本地档迁移机制

### 验收标准

- 已有本地进度的测试用户不会因为登录而丢档

---

## Step 12：断网与异常兜底

### 目标

保证“网络差也能玩”，避免账号系统让体验变脆。

### 需要完成的事

1. 增加在线状态检测
2. 云端写入失败时：
   - 保留本地成功结果
   - 记录同步失败状态
3. 云端读取失败时：
   - 回退本地存档
4. 增加用户提示：
   - 不是红色报错堆栈
   - 而是“本机已保存，待网络恢复后再同步”

### 产出

- 网络异常兜底机制

### 验收标准

- 断网时游戏仍可保存本地
- 恢复网络后可继续使用账号与云档

---

## Step 13：接入 GitHub Actions + CloudBase 静态托管发布链路

### 目标

把前端真正部署到 CloudBase 静态网站托管，并将发布链路切成 GitHub Actions 自动部署。

### 需要完成的事

1. 在仓库中新增 GitHub Actions 工作流
2. 工作流固定使用：
   - `Node.js 22`
   - `npm ci`
   - `npm run build`
   - `tcb hosting deploy ./dist -e <envId>`
3. 在 GitHub 仓库配置 Secrets：
   - `TCB_SECRET_ID`
   - `TCB_SECRET_KEY`
   - `TCB_ENV_ID`
4. 首次 push 后验证工作流能自动触发
5. 确认 CloudBase 静态托管内容被最新构建覆盖
6. 不再依赖控制台 Git 部署的自动触发能力

### 推荐核对项

1. 构建命令包含故事脚本构建
2. 产物目录与当前项目一致
3. 环境变量不写死到源码
4. 工作流使用仓库 Secrets 注入腾讯云凭证
5. 发布链路不依赖浏览器手动点“重新部署”

### 产出

- 可自动发布的 CloudBase 静态托管站点
- 仓库内可维护的 GitHub Actions 工作流

### 验收标准

- 通过 CloudBase 静态托管默认域名可打开游戏首页
- push 到 `master` 后会自动触发部署
- 认证与云存档接口不会因默认域名未加入跨域设置而报来源错误

---

## Step 14：验证默认域名可访问

### 目标

把测试站点推进到“可用默认域名访问”的状态。

### 需要完成的事

1. 记录 CloudBase 静态托管默认域名
2. 将默认域名加入 CloudBase 跨域设置
3. 用默认域名打开前端站点
4. 验证注册、登录与后续云接口不会被域名校验拦截
5. 验证 GitHub Actions 部署后的新版本已实际生效

### 产出

- 可稳定用于当前阶段测试与内测的默认访问域名

### 验收标准

- 默认域名可打开
- CloudBase 来源校验通过
- 不依赖 EdgeOne 3 小时预览链接

---

## Step 15：全链路测试

### 目标

在正式发布前，把最关键的用户路径全部走通。

### 需要完成的事

至少测试以下场景：

1. 手机浏览器首次打开站点
2. 未登录试玩并本地存档
3. 注册账号
4. 登录账号
5. 上传本地档到云端
6. 在另一台设备登录并读回云档
7. 退出登录
8. 找回密码
9. 断网后保存
10. 恢复网络后继续同步

### 测试设备建议

1. 一台手机
2. 一台电脑
3. 至少两个不同浏览器

### 产出

- 一套正式发布前的联调结果

### 验收标准

- 账号、云存档、移动端访问都通过
- 没有“只能在本地 dev 环境可用”的残留问题

---

## Step 16：回修文档与发布说明

### 目标

让项目文档跟上新能力，避免后续维护混乱。

### 需要完成的事

1. 更新进度文档
2. 更新发布文档
3. 更新存档说明
4. 明确用户须知：
   - 未登录只保存在本机
   - 登录后可云同步
   - 当前阶段建议使用 CloudBase 默认域名访问

### 建议回修文件

- `scripts/schedule.md`
- `README` 或发布说明文档
- 存档相关说明文档

### 产出

- 一致的项目文档口径

### 验收标准

- 文档不再停留在“纯本地存档”时代

---

## 推荐执行顺序

按以下顺序推进，不建议跳步：

1. Step 1：锁定实施口径
2. Step 2：准备腾讯云资源
3. Step 3：配置认证
4. Step 4：设计云存档结构
5. Step 5：配置安全规则
6. Step 6：前端初始化 SDK
7. Step 7：接入登录注册
8. Step 8：接入找回密码
9. Step 9：封装云存档数据层
10. Step 10：改造游戏菜单与槽位面板
11. Step 11：本地档迁移
12. Step 12：网络异常兜底
13. Step 13：接入 CloudBase 静态托管
14. Step 14：验证默认域名可访问
15. Step 15：全链路测试
16. Step 16：回修文档

---

## 最终验收标准

当以下条件全部满足时，`020` 视为完成：

1. 用户可注册账号
2. 用户可登录
3. 用户可退出登录
4. 用户可找回密码
5. 每个登录用户拥有独立 6 个云存档槽位
6. 用户 A 无法看到用户 B 的存档
7. 本地存档仍然存在并可兜底
8. 玩家可在手机和电脑上读取同一账号的云存档
9. 中国大陆普通用户可通过正式网页链接访问
10. 项目文档已同步更新

---

## 备注

`020` 是实施拆分文档，不是代码变更文档。  
后续真正开工时，应按 step 逐步执行，而不是直接同时改前端、云端和发布配置。
